import React from 'react';
import styled from 'styled-components';

const pStyles = styled.p`
    width: 100px;
    text-align: center;
`;

const App = props => {
  <html>
    <body>
      <pStyles>
        I'm an SSR web-app
      </pStyles>
    </body>
  </html>;
};

export default App;
